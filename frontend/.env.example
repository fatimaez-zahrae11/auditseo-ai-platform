# Public frontend configuration only. Every VITE_* value is visible in the browser bundle.
VITE_API_BASE_URL=http://localhost:8000/api

# Production must use an absolute HTTPS API URL.
# Example: VITE_API_BASE_URL=https://api.production-domain.com/api
