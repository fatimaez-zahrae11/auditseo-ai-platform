# Public, non-secret frontend metadata only. Vite values are visible in the browser bundle.
VITE_APP_NAME="AuditSEO AI Platform"

# Required in production and must use HTTPS. Local HTTP loopback is development-only.
# Production example: VITE_API_BASE_URL="https://api.auditseo.example/api"
VITE_API_BASE_URL="http://127.0.0.1:8000/api"
