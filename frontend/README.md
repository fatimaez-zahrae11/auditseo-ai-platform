# AuditSEO AI Platform frontend

Production React interface for the Audit Workspace and administration control center, integrated with the protected Laravel API for authentication, audits, recommendations, analytics, and monitoring.

## Local development

```bash
npm install
npm run dev
```

Use `npm run lint` for TypeScript validation and `npm run build` for the production bundle.
